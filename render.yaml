services:
  - type: web
    name: fieldflow-ortools
    runtime: python
    plan: free
    region: frankfurt
    rootDir: solver_service
    buildCommand: pip install -r requirements.txt
    startCommand: uvicorn app:app --host 0.0.0.0 --port $PORT
    healthCheckPath: /health
    autoDeployTrigger: commit
